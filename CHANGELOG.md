# 変更履歴（CHANGELOG）

観測所ファインダーの開発フェーズごとの変更履歴です。新しいものが上に来る順に並んでいます。
現在の機能概要は [README.md](./README.md) を参照してください。

- ~~**フェーズ29**: Colab一括DL: 10分値をGoogleドライブにキャッシュしセッション切れから再開できるように~~ ✅ 完了
  - 10分値は地点数×日数ぶんのリクエストが必要で長時間かかるため、Colabのセッション切れ（アイドルタイムアウト等）に
    遭うとローカルディスクごと消え、それまでの取得がすべて無駄になっていた。「まとめ方」の下に
    「Googleドライブに保存し、セッション切れ後の再実行は続きから取得する」チェックボックス（既定でON）を追加し、
    取得できた「1地点×1日」ぶんを`MyDrive/jma_10min_cache`に小さなCSVとして保存するようにした
  - 再実行時は各地点・各日についてまずこのキャッシュを確認し、あればHTTPリクエストも待機時間も発生させずに
    そのまま利用する（`read_ten_min_cache`/`write_ten_min_cache`/`mount_drive_for_cache`）。ドライブのマウントに
    失敗した場合は再開機能なしで通常どおり取得を続ける
  - 結果サマリに「キャッシュから再利用」件数を追加した

- ~~**フェーズ28**: Colab一括DL: 10分値の取得に対応~~ ✅ 完了
  - 「データの種類」に「10分値」を追加した。10分値は気象庁「過去の気象データ・ダウンロード」(obsdl)のCSV APIには
    集計期間が無く、姉妹サイトの「過去の気象データ検索」(etrn)の表示ページ（気象官署は`view/10min_s1.php`、
    アメダスは`view/10min_a1.php`）からしか取得できないため、既存のobsdl経路とは別に、1地点×1日ごとにHTMLの
    データテーブルを取得してCSVへ変換する経路を追加した
  - HTMLテーブルの解析は`html.parser.HTMLParser`を使った自製の簡易パーサー（`_TableExtractor`）で行い、
    colspanを展開しつつヘッダー行（項目名・単位）を動的に読み取って列ラベルを組み立てる。地点種別によって
    観測項目（列構成）が異なるため、列を固定せずページ側の見出しに追従する作りにした
  - 10分値は項目の個別選択ができない（ページに載っている項目をそのまま全部取得する）ため、フォームの
    「観測項目」チェックボックスと「まとめ方」ラジオボタンは10分値選択時は非表示にし、代わりに専用の説明文
    （`elem_note_10min`・`merge_note_10min`）を表示するようにした。まとめ方は常に「地点ごとに1ファイル」に固定
  - 1地点×1日を1リクエストとするため、期間の分割（`build_plan`等）は使わずリクエスト数は単純に「地点数×日数」。
    件数が多い（2000件超）場合は実行前にその旨を警告表示する

  - 流れ星の軌道（左上→右下）自体は正しかったが、`.shooting-star`の`linear-gradient`が
    `90deg, 不透明, 透明`（要素のローカル左端が不透明・右端が透明）になっており、回転角と
    `translate`の向きの組み合わせ上、進行方向の先頭側が透明・後方が不透明になっていた
    （＝先頭に何も無く、後ろに光点が付いてくる「後退しているように見える」形）。
    グラデーションの並びを`透明, 不透明`に反転し、進行方向側を明るく・後方を透明にした
  - 3本の流れ星のアニメーション周期を2倍にした（`.shooting-star--1`: 9s→18s、`--2`: 14s→28s、
    `--3`: 17s→34s）。`animation-delay`も同じ比率で2倍にし（-1s→-2s、-6s→-12s、-11s→-22s）、
    3本の相対的なタイミングのずれは変えていない
- ~~**フェーズ26**: ヘッダーを夜空デザインに変更（星の瞬き・流れ星）~~ ✅ 完了
  - `.site-header`の背景を、従来の単色＋等圧線風の装飾ラインから、濃紺〜黒の放射状グラデーションに変更した
  - `.site-header::before`に複数の`radial-gradient`を重ねて小さな光点（星）を散りばめ、`background-size`でタイル状に繰り返し、`twinkle`アニメーション（不透明度を往復）で瞬かせている
  - 流れ星は`.shooting-star`要素3本（`index.html`にaria-hidden付きで配置）。それぞれ角度・長さ・アニメーション周期（9s/14s/17s）と開始位置をずらし、`animation-delay`に負の値を与えて非同期に見えるようにした。周期の90%以上は不透明度0（静止）で、終盤の数%だけ`translate`で斜めに移動しながら現れて消える＝「たまに流れる」演出。`transform: rotate()`（静的な角度）と`translate`（動的な移動量）を別プロパティとして扱うことで、両方を同時に効かせている
  - `prefers-reduced-motion: reduce`環境では瞬き・流れ星のアニメーションを止め、静止した星空にする
- ~~**フェーズ25**: レイアウト微調整（沖縄・南極の通常サイズ化・観測要素パネルの高さ揃え）~~ ✅ 完了
  - フェーズ23で北海道が2枚のカードになったことに伴い、5×2グリッドの帳尻合わせとして沖縄・南極を`.region-group-stack`で半分の高さに重ねて表示していたが、フェーズ24で北海道が1枚に統合されグリッドが自然に収まるようになったため不要になった。`regionSelector.js`の`buildDisplayRegions()`から沖縄・南極を特別扱いする`stack`ロジックを削除し、他の地方と同じ通常サイズのカードとして描画するようにした。`css/components.css`の`.region-group-stack`も削除した
  - 「観測要素で絞り込む」パネルの高さを、「種別で絞り込む」＋「廃止済み観測所」を縦積みにした右列の高さに揃えた。`css/layout.css`の`.filter-panels__row`から`align-items: start`を削除し、CSS Gridの既定値`align-items: stretch`に戻すことで、同じグリッド行内の各アイテムが自動的に最も高いアイテム（右列）に高さを合わせるようにした
- ~~**フェーズ24**: レイアウト微調整（北海道カードの統合・観測要素/種別/廃止済みパネルの配置・URLコピー機能の削除）~~ ✅ 完了
  - ステータスバーの「この条件のURLをコピー」ボタンを削除した（ブラウザのアドレスバーのURLで代用できるため）。`index.html`から`#copy-link-btn`とその親の`.status-bar__right`を、`main.js`から`copyLinkBtn`の参照とクリックハンドラを、`css/layout.css`から未使用になった`.status-bar__right`のスタイルを削除した
  - 北海道の地域カードを、フェーズ23の「北海道（道北・道東）」「北海道（道央・道南）」2枚組から、宗谷・上川など14地域をまとめて持つ1枚に統合した。`regionSelector.js`の`buildDisplayRegions()`を変更し、`hokkaidoSubAreas`の2グループを`flatMap`で1つの地域リストにまとめてから単一の region オブジェクトとして扱うようにした（`filterEngine.js`側のprecNoベースの絞り込みロジックは変更なし）。5×2グリッドではカードが1枚減った分、近畿が1段目に戻っている
  - 絞り込みパネルの配置を「観測要素で絞り込む」を左列、「種別で絞り込む」「廃止済み観測所」を右列に縦積みにする形に変更した（フェーズ22で導入した3パネル横並びから変更）。`index.html`に`.filter-panels__row-right`のラッパーを追加し、`css/layout.css`の`.filter-panels__row`を`grid-template-columns: repeat(3, 1fr)`から`1fr 1fr`の2カラムに変更した
- ~~**フェーズ23**: 地域選択の改善（北海道14地域分割・選択クリアボタンの配置）~~ ✅ 完了
  - 北海道は面積が広く1都道府県のままでは絞り込みの単位として粗すぎたため、気象官署のprecNo（府県予報区番号。11〜24が北海道内に対応）を使って宗谷・上川・留萌・石狩・空知・後志・オホーツク・胆振・日高・十勝・釧路・根室・渡島・檜山の14地域に分割した。`data/stations.json`に新しい`hokkaidoSubAreas`（2グループ×7地域）を追加し、`scripts/build_stations.py`が再生成時も引き継ぐようにした。`station.prefecture`自体は「北海道」のまま変更していないため、CSV出力・気象庁リンク・キーワード検索・「地方」列表示への影響はない
  - `filterEngine.js`に`getHokkaidoArea()`（precNoから地域名を求める）と`regionSelectorKey()`（地域選択の絞り込み・集計に使うキー。北海道は地域名、それ以外は都道府県名）を追加し、`computeVisibleStations()`・`buildPrefectureCounts()`をこのキーで判定するように変更した
  - `regionSelector.js`に`hokkaidoSubAreas`パラメータを追加。渡されると北海道の単一カードの代わりに「北海道（道北・道東）」「北海道（道央・道南）」の2枚（各7地域）を描画する。5×2グリッドではこの1枚増加分だけ近畿が2段目に押し出されるため、選択肢が1件ずつしかない沖縄・南極を`.region-group-stack`で同じ列に半分の高さで重ねて表示し、グリッド全体を10枚に保つようにした
  - 「47都道府県一括選択」「すべて一括選択」の対象件数が、北海道の分割に伴い47/48から60/61に変わった（北海道が1都道府県から14地域になったため）
  - 「観測要素で絞り込む」「種別で絞り込む」パネルの「選択をクリア」ボタンを、パネル本文内からタイトル横の「i」アイコンの右隣に移動した。`elementFilter.js`/`typeFilter.js`に`clearButtonSlot`パラメータを追加し、渡されるとそちらにボタンを描画するようにした（省略時は従来通りパネル本文内に描画され後方互換）
- ~~**フェーズ22**: レイアウト改善（地域パネルの5×2化・パネル配置・説明文のホバー表示・地図既定表示の修正）~~ ✅ 完了
  - 「地域で絞り込む」パネルを横3×縦4（`.region-list`のgrid-template-columns: repeat(3, 1fr)）から横5×縦2（repeat(5, 1fr)）に変更した（`css/components.css`）
  - パネル配置を再構成: 地域パネルを全幅の1段目に、観測要素・種別・廃止済み観測所の3パネルをその下に横並びの1段（`.filter-panels__row`）で配置するようにした（フェーズ21の右側縦積みレイアウトから変更。`index.html` / `css/layout.css`）
  - 各パネルの説明文（`.panel__note`）を既定で非表示にし、タイトル横に追加した「i」アイコン（`.panel__info-icon`）にカーソルを合わせる／フォーカスすると吹き出しとして表示するようにした。CSS のみで実装（`.panel__info:hover .panel__note` 等）し、キーボード操作でも`tabindex="0"`＋`:focus-within`で表示できる（`css/layout.css`）
  - 地図の既定表示（絞り込みなしの初期状態）が南極・昭和基地まで含めて世界地図規模にfitBoundsされてしまう不具合を修正。原因は、フェーズ21で廃止済み観測所が既定で母集団に含まれるようになったことで、`mapView.js`内の「絞り込みが効いているか」の判定（`visibleStations.length !== allStations.length`）が、実際には何も絞り込んでいなくても真になってしまっていたため。判定を地域・観測要素・種別・検索語の実際の選択状態だけで行う`hasActiveFilters()`に置き換え、既定表示では表示範囲を動かさない（日本周辺の初期ビューのまま）ようにした（`mapView.js`）
- ~~**フェーズ21**: UI/UX改善（一括選択・パネル配置・廃止済み観測所の既定表示化 等）~~ ✅ 完了
  - 地域選択に「47都道府県一括選択」を追加。従来の「すべて一括選択」（南極含む48地域）とは別に、日本国内の47都道府県だけを一括選択できるようにした（`regionSelector.js`）
  - 「観測要素で絞り込む」パネル下の余白を解消するため、「種別で絞り込む」「廃止済み観測所」パネルを右側の縦積みレイアウトに移動した（`index.html` / `css/layout.css`の`.filter-panels__right`）
  - 観測所一覧の観測要素タグを、要素の有無に関わらず6要素分の固定位置に表示するよう変更（CSS Gridと非表示プレースホルダーを使用）。従来は観測要素が少ない地点でタグが左詰めになり列がずれて見えていた（`stationList.js`の`renderElementTags`、`css/components.css`の`.element-tags`）
  - 廃止済み観測所の既定を「含めない」から**「含める」に反転**した。チェックボックスは「廃止済みの観測地点を含めない」というオプトアウトの向きに変わり、（　）内の件数も他の絞り込み条件（地域・観測要素・種別・検索語）に連動して変化するようにした（`discontinuedFilter.js`・`stateManager.js`・`main.js`）。URLクエリ`discontinued`パラメータの意味も反転し、既定（含める）のときは省略、明示的に除外するときだけ`discontinued=0`を付与するようにした（`urlState.js`）
  - 湿度・日照時間の観測地点数が現役観測所のみでは844件ずつで完全に一致していたため列の統合を検討したが、廃止済み観測所を含めた既定状態では日照時間942件・湿度857件とズレて観測地点の集合も一致しなくなることが判明したため、統合は見送り現状の個別表示を維持することにした
- ~~**フェーズ20**: 残り139地点の手動確認結果を反映~~ ✅ 完了
  - ユーザーが気象庁の地点選択ページで139地点全てを目視確認した結果: (b) 複数世代の19地点は「候補のblockNoで合っている」ことを確認（同じ物理的な場所を指すため、地図上のマーカーは1つしかなく、どの世代を指すか気にせずそのマーカーの番号でよいと判断）。(a) 現役同名の99地点は「地図上に現役の地点しか無い」ことを確認（＝廃止済み地点自身の番号は既に残っていない）。(c) 該当なしの21地点は「地図上に該当する地点自体が無い」ことを確認
  - (b)の19地点にblockNoを確定し、廃止済み観測所のprecNo/blockNo確定数は386地点になった。(a)(c)の120地点は気象庁の地点選択ページ自体に情報が残っていないため、引き続き未確定（気象庁ページへのリンクなし）とする
  - 目視確認の記録として使った`discontinued_stations_to_fill.csv`はリポジトリに残している
- ~~**フェーズ19**: 廃止済み観測所のprecNo/blockNo確認~~ ✅ 完了
  - ユーザーが気象庁の地点選択ページ（`prefecture.php`の地図リンク）から`document.querySelectorAll('map area')`でprec_no/block_noを収集する方法を教えてくれた。これは`fetch_block_numbers.py`が使っているのと同じページで、実は既存の`data/block_numbers.json`に廃止済み観測所の情報も紛れ込んでいることが分かった（1,678件収録されており、現役1,287地点より391件多い）
  - 廃止済み506地点を地点名・都道府県で`block_numbers.json`と突き合わせたところ485件が一致した。ただしそのうち105件は**現役観測所と同名**で、一致したblock_noが現役側に既に設定済みの値と完全一致することを確認（＝廃止済み地点自身の番号ではなく現役側を指している）。さらに19件は同名の観測所が複数の時期（世代）にまたがっており、1つのblock_noがどの世代を指すか特定できなかった
  - これらの紛らわしいケースを除いた361地点でprecNo/blockNoを確定した
  - `mapView.js`のポップアップ表示を修正し、廃止済み観測所でもprecNo/blockNoが確定していれば通常の観測所と同じ気象庁リンクを表示しつつ、「廃止済み観測所（観測期間: ...）」の注記も併記するようにした（`stationList.js`側は元々precNo/blockNoの有無だけでリンク要否を判定していたため、追加の変更なしで正しく動作した）
  - さらにユーザーから2017年時点の観測地点一覧（個人ブログ公開のcsvをtxt化したもの、1,664地点）が提供された。これを未確定の地点と突き合わせたところ、フェーズ8/14で新旧番号の切り替えと判明していた7地点のうち6地点（弟子屈・神津島・高遠・三倉・宇奈月・新島のアメダス期）について、「2候補のうち現役側が使っていない方」を消去法で確定できた（残る1件・新島の測候所期（1961〜1966年）はアメダス制度開始(1974年)より前に廃止されているため対象外とした）
  - この時点で367地点でprecNo/blockNoを確定。残り139地点（現役同名99・複数世代19・該当なし21）はCSV（`discontinued_stations_to_fill.csv`）に書き出し、ユーザーに手動確認を依頼した
- ~~**フェーズ18**: 地図のCDN依存を解消~~ ✅ 完了
  - Leaflet / Leaflet.markercluster を`npm install`で取得し、`dist`配下の必要なファイル（本体JS/CSS・マーカー画像・クラスタリングのCSS/JS）を`vendor/leaflet/`としてリポジトリに同梱した
  - `index.html`の参照先を`https://unpkg.com/...`から`vendor/leaflet/...`に変更し、SRI（integrity/crossorigin属性）は同一オリジンの自前ファイルのため撤去した
  - これにより、外部ネットワークが遮断された環境（社内LAN・CDNブロックなど）でも地図が表示されるようになった。バージョンを上げる場合は`npm install leaflet@<version> leaflet.markercluster@<version>`のうえ`vendor/leaflet/`を再コピーする
- ~~**フェーズ17**: 廃止済み観測所データの正確化・大幅拡充~~ ✅ 完了
  - ユーザーから気象庁純正の**アメダス地点情報履歴ファイル（`amdmaster.index`の完全版CSV、7,935レコード）**が提供され、これを解析した
  - 地点番号ごとに観測開始日・終了日・観測要素（気温/降水量/風/日照時間/積雪/湿度をフラグで明示）の全履歴が入っており、フェーズ16で推測に頼っていた部分（観測要素の構成、筑波山・白浜・金武が実際には期間の異なる2レコードに分かれていたこと等）を正確な値に置き換えられた
  - 地点番号は全1,868件（現役1,286件＋廃止1,868-1,286=582件）。ただし582件のうち88件は「番号が変わっただけで観測は継続」（例: 札幌14161→14162→14163、旭川12441→12442）と判明したため、`Old Station Number`列を使って後継地点への付け替えを追跡し、本当に廃止された**494件**を特定した
  - 都道府県の判定は`scripts/build_stations.py`と同じ方式（[dataofjapan/land](https://github.com/dataofjapan/land)の境界GeoJSON + point-in-polygon、海岸部5件は最近傍で補完）で行った
  - フェーズ16の気象官署・測候所12地点（`smaster.index`由来。アメダス網の外にある独立した観測所のため今回のアメダス履歴には含まれない）はそのまま残し、494件と合わせて**廃止済み観測所は合計506地点**になった
  - 山頂の測候所（筑波山・伊吹山・剣山・阿蘇山）、1902年の大噴火で職員全員が犠牲になった鳥島に加え、1974年のアメダス発足直後に試験的に置かれ数年で役目を終えた地点なども含む
  - 地点番号（precNo/blockNo）は今回もetrn側の対応が未確認のため引き続き未設定。標高は`amdmaster.index`の値をそのまま収録した
- ~~**フェーズ16**: 廃止済み観測所の追加（第一弾）~~ ✅ 完了 → フェーズ17でデータを全面的に作り直し
  - 気象庁「過去の気象データ・ダウンロード」ヘルプ（`/risk/obsdl/top/help1`）に「約1,500地点（現在観測を行っていない約200地点を含む）」という記述があるが、`data/stations.json`の元データ（アメダス観測所表`amedastable.json`）は現役ネットワークのみを収録しており、この約200地点は一件も含まれていない
  - ユーザーから提供いただいた資料（気象官署・測候所の履歴ファイル`smaster.index`、アメダス観測所履歴を要約したPDF）から、まず21地点（気象官署・測候所12地点＋アメダス9地点）を`discontinuedStations`として追加した
  - 一覧の「廃止」タグ・観測期間表示、地図のグレーマーカー、CSVの「状態」「観測期間」列、URLクエリ`discontinued=1`など、UI・データ構造の土台をここで作った
- ~~**フェーズ15**: マーカー⇔一覧行の相互連携~~ ✅ 完了
  - 地図のマーカーをクリックすると、対応する一覧の行が表示されるページへ自動的に切り替わり、ハイライト＆スクロール表示される
  - 一覧の行をクリックすると、地図がその地点へパンしてポップアップを開く（クラスタ化中はクラスタを解いてから開く）
  - 両者は`store.selectedStationId`という共通の状態でつながっており、絞り込み変更や手動でのページ送りでは選択を解除する
- ~~**フェーズ14**: 残り7地点のblock_no手動確定~~ ✅ 完了
  - フェーズ8で候補記録のまま保留していた7地点（弟子屈・新島・神津島・高遠・三倉・宇奈月・有川）について、気象庁の地点選択ページで手動確認を実施
  - 2つのblock_no候補は重複ではなく、観測要素の追加（雨量のみ→4要素化）または観測所の移転（統計切断）による新旧番号の切り替えと判明。現在有効な新番号側を`precNo`/`blockNo`として確定した
  - `merge_block_numbers.py`は、すでに`precNo`/`blockNo`が確定済みの観測所（`scripts/extra_stations.json`由来、および今回手動確認した7地点）を再照合の対象から除外するように変更し、再実行時に手動確認の結論が上書きされないようにした
  - 結果、全1,287地点で`precNo`/`blockNo`が確定し、一覧・地図とも例外なく地点別リンクを表示するようになった
- ~~**フェーズ12**: 一覧の情報量改善~~ ✅ 完了
  - 「プリセットで絞り込む」パネルを削除（個別パネルで同じ絞り込みができるため）
  - 一覧の「気象庁」列を廃止し、地点名自体をリンク化。リンク先を`view/daily_s1.php`から`stats/etrn/index.php`（地点選択済みのトップ）へ変更し、アメダス1,230地点でエラーページになっていた問題を解消。地点番号未確定の7地点も都道府県ページへ案内し、全地点が有効なリンクになった
  - 一覧に かな・標高(m)・緯度・経度 を追加（全地点で欠測なし）。CSVにも同じ列＋気象庁ページURLを追加
  - 観測要素を6要素それぞれ色分けし、観測要素フィルタの色ドットを凡例とした
- ~~**フェーズ11**: 操作性の改善・南極追加~~ ✅ 完了
  - 地図の操作にCtrl（macOSは⌘）を要求するジェスチャ制御を追加（ページスクロール中に地図が勝手に動く問題の解消）。併せて「検索結果に合わせる」ボタンを追加し、絞り込みが無いときは表示範囲を自動調整しないようにした
  - 南極・昭和基地（prec_no=99 / block_no=89532）を観測所マスタに追加し、地方「南極」を新設。再生成時にも失われないよう `scripts/extra_stations.json` に定義を置き、`build_stations.py` / `merge_block_numbers.py` を対応させた
  - 地域・観測要素・種別の「(件数)」を、他の絞り込み条件に連動して更新するように変更（`buildFacetCounts()`）。0件になった選択肢は薄いグレーで表示する
- ~~**フェーズ10**: 本番品質への仕上げ~~ ✅ 完了
  - ユーザー向け表示から内部的な「フェーズN」表記を除去（開発時の進捗管理用の呼び方であり、利用者には無関係なため）
  - ステータスバー・観測所一覧の説明文を実際のデータ状態（1,279/1,286地点でリンク確定、残り7件は候補あり表示）に合わせて修正
  - ステータス件数表示に`aria-live="polite"`を付与し、絞り込み結果の変化をスクリーンリーダーでも把握できるように
  - Leaflet / Leaflet.markercluster のCDN読み込みに、npm配布物から算出した実際のSRI（sha384）ハッシュを付与し、CDN側の内容改ざんに対する保護を追加
  - `.gitignore`を追加（`node_modules/`等が誤ってコミットされることを防止）
- ~~**フェーズ9**: 絞り込み条件のプリセット機能（気象官署のみ／アメダスのみ／気温・降水量ともに観測 等をワンクリック適用、種別フィルタ軸を新設）~~ ✅ 完了（プリセット機能はフェーズ12で削除）
- ~~**フェーズ8**: precNo/blockNo未収録10地点の手動補完（表記ゆれ3件は自動解消、重複block_no 7件は候補を記録し保留）~~ ✅ 完了
- ~~**フェーズ7**: 絞り込み状態のURLクエリ化（地域・観測要素・AND/OR・検索語・ページ番号を復元可能に、「URLをコピー」ボタン）~~ ✅ 完了
- ~~**フェーズ6**: 地図表示（Leaflet、気象官署/アメダス色分け、マーカークラスタリング、絞り込み結果との連動）~~ ✅ 完了
- ~~**フェーズ5**: 全観測所データの整備（`precNo`/`blockNo`含む実データ化）、UI/UXブラッシュアップ（検索・ページネーション・ヘッダー固定）~~ ✅ 完了
- ~~**フェーズ4**: 選択結果のCSVエクスポート、気象庁ページへのリンク生成~~ ✅ 完了
- ~~**フェーズ3**: 観測要素による絞り込み（AND/OR切り替え対応）~~ ✅ 完了
- ~~**フェーズ2**: 地方・都道府県単位の一括選択（階層チェックボックス）~~ ✅ 完了
