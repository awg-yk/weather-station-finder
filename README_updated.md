# 観測所ファインダー（Weather Station Finder）

気象庁の「過去の気象データ・ダウンロード」を、研究・解析向けに使いやすくするための非公式ツールです。
気象庁サイトの置き換えではなく、公開データをより効率よく取得するための補助ツールを目指します。

## 現在の状態: フェーズ26「ヘッダーを夜空デザインに変更（星の瞬き・流れ星）」実装済み（フェーズ25「レイアウト微調整（沖縄・南極の通常サイズ化・観測要素パネルの高さ揃え）」・フェーズ24「レイアウト微調整（北海道カードの統合・観測要素/種別/廃止済みパネルの配置・URLコピー機能の削除）」・フェーズ23「地域選択の改善（北海道14地域分割・選択クリアボタンの配置）」・フェーズ22「レイアウト改善（地域パネルの5×2化・パネル配置・説明文のホバー表示・地図既定表示の修正）」・フェーズ21「UI/UX改善（一括選択・パネル配置・廃止済み観測所の既定表示化 等）」・フェーズ20「廃止済み観測所139地点の手動確認結果を反映」・フェーズ19「廃止済み観測所のprecNo/blockNo確認」・フェーズ18「地図のCDN依存を解消」・フェーズ17「廃止済み観測所データの正確化・大幅拡充」・フェーズ16「廃止済み観測所の追加（第一弾）」・フェーズ15「マーカー⇔一覧行の相互連携」・フェーズ14「残り7地点のblock_no手動確定」・フェーズ12「一覧の情報量改善」・フェーズ11「操作性の改善・南極追加」・フェーズ10「本番品質への仕上げ」・フェーズ8「precNo/blockNo手動補完」・フェーズ7「URLクエリ化」・フェーズ6「地図表示」・フェーズ5「実データ整備」は完了。フェーズ9のプリセット機能はフェーズ12で廃止）

- **ヘッダー（「観測所ファインダー」の背景）を夜空デザインに変更**しました。濃紺〜黒のグラデーションに、
  小さな光点を散りばめて瞬かせ、ときおり流れ星が3本それぞれ異なる周期・角度で流れます。画像やJSは使わず
  CSSのグラデーション・アニメーションのみで実装しています。`prefers-reduced-motion`が有効な環境では
  瞬き・流れ星のアニメーションを止めます（フェーズ26）。
- **沖縄・南極のカードを他の地方と同じ大きさに戻し**ました。フェーズ23で北海道が2枚のカードになった際、
  5×2グリッドを維持するために沖縄・南極を半分の高さで重ねて表示していましたが、フェーズ24で北海道が
  1枚に統合されグリッドの帳尻合わせが不要になったため、重ね表示をやめて通常のカードに戻しています（フェーズ25）。
- **「観測要素で絞り込む」パネルの高さを、「種別で絞り込む」＋「廃止済み観測所」を縦積みにした右列の高さと
  揃え**ました。CSS Gridの`align-items: stretch`（既定値）を使い、右列の高さに左列を自動的に合わせています（フェーズ25）。
- **ステータスバーの「この条件のURLをコピー」ボタンを削除**しました。ブラウザのアドレスバーのURLで代用できるためです（フェーズ24）。
- **北海道の地域カードを1枚に統合**しました。フェーズ23で「北海道（道北・道東）」「北海道（道央・道南）」の
  2枚に分割していましたが、1枚のカードに宗谷・上川など14地域をまとめる形に変更しています
  （地域選択・件数集計のロジック自体はフェーズ23のまま変わらず、表示のグルーピングだけを変更。フェーズ24）。
- **「観測要素で絞り込む」を左、「種別で絞り込む」「廃止済み観測所」を右に縦積みで配置**するようにしました
  （フェーズ22で3パネル横並びにしていたものを変更。フェーズ24）。
- **「北海道」を宗谷・上川・留萌・石狩・空知・後志・オホーツク・胆振・日高・十勝・釧路・根室・渡島・檜山の
  14地域（振興局相当）に分割**しました。都道府県1件のままでは北海道内の絞り込み単位として粗すぎたための
  変更です（フェーズ23。表示上のカード枚数はフェーズ24で1枚に統合）。
- **「観測要素で絞り込む」「種別で絞り込む」パネルの「選択をクリア」ボタンを、タイトル横の「i」アイコンの
  右隣に移動**しました（フェーズ23）。
- **「地域で絞り込む」パネルを横5×縦2のグリッドに変更**しました（従来は横3×縦4）。パネルも全幅表示にし、
  その下に「観測要素で絞り込む」「種別で絞り込む」「廃止済み観測所」の3パネルを横並びで配置しています（フェーズ22）。
- **各パネルの説明文（長文の注記）を既定で非表示にし、タイトル横の「i」アイコンにカーソルを合わせた（または
  フォーカスした）ときだけ吹き出しで表示する**ようにしました（フェーズ22）。
- **地図の既定表示が日本周辺のままになるよう修正**しました。廃止済み観測所が既定で含まれるようになった
  （フェーズ21）ことで、絞り込みが何も無い状態でも「絞り込みが効いている」と誤判定され、南極・昭和基地まで
  表示範囲に含めようとして地図が世界全体まで引いて表示されてしまう不具合がありました。絞り込みの判定を
  地域・観測要素・種別・検索語の実際の選択状態だけで行うよう修正し、既定表示では表示範囲を動かさない
  （＝日本周辺のまま）ようにしています（フェーズ22）。
- **廃止済み観測所506地点が既定で一覧・地図・件数集計に含まれる**ようになりました（従来はオプトインでオフが既定）。
  チェックボックスは「廃止済みの観測地点を含めない」というオプトアウトの向きに変わり、（　）内の件数は他の
  絞り込み条件（地域・観測要素・種別・検索語）に連動して変化します（フェーズ21）。
- **地域選択に「47都道府県一括選択」を追加**しました。従来の「すべて一括選択」（南極を含む48地域が対象）とは
  別に、日本国内の47都道府県だけを一括選択できます（フェーズ21）。
- **観測所一覧の観測要素タグの列位置を固定**しました。従来は観測要素が少ない地点でタグが左詰めになり列がずれて
  見えていましたが、要素ごとに固定位置へ表示するよう変更しています（フェーズ21）。

- **現在は運用されていない過去の観測所506地点（廃止済み観測所）を追加**しました。気象庁純正のアメダス地点情報履歴ファイルを解析し、番号の付け替え（同一地点の継続運用）を除いた「本当に廃止された」地点を特定しています。既定で現役の1,287地点と合わせて一覧・地図に表示され、「廃止済み観測所」パネルのチェックボックスで除外できます（フェーズ16・17・21）。
- **地図のマーカーと一覧の行が相互に連動**するようになりました。マーカーをクリックすると一覧の該当行がハイライト表示され、行をクリックすると地図がその地点へ移動します（フェーズ15）。
- **全1,287地点で気象庁の地点番号（`precNo`/`blockNo`）が確定**しました。フェーズ8で候補記録のまま保留していた7地点は、新旧番号の切り替え（観測要素の追加・観測所の移転）と判明したため、現在有効な番号を確定しています（フェーズ14）。

- **観測所マスタデータを全国1,286地点（気象官署56地点＋アメダス1,230地点）に刷新**しました（旧: 都道府県代表47地点のサンプルデータ）。さらに**南極・昭和基地**を追加し、収録数は1,287地点です。
- **地図はCtrl（macOSは⌘）を押しながらでないと動かない**ようにしました。ページのスクロール中に地図が勝手に拡大縮小・移動してしまう問題への対処です（フェーズ11）。
- **各絞り込みUIの「(件数)」が、他の絞り込み条件に連動する**ようになりました。例えば地域で北海道だけを選ぶと、観測要素・種別の件数も北海道内の件数に変わります（フェーズ11）。
- **地方単位・都道府県単位の一括選択**（階層チェックボックス、indeterminate状態対応、全国一括選択、クリアボタン）を実装済みです。
- **観測要素（気温・降水量・積雪・風・湿度・日照時間）による絞り込み**（AND/OR切り替え、クリアボタン）を実装済みです。
- 地域フィルタと観測要素フィルタは組み合わせて使えます（両方AND条件で合成）。
- **現在の絞り込み結果をCSVとしてダウンロード**できます（Excelでの文字化け対策としてUTF-8 BOM付き）。
- **一覧の地点名が気象庁「過去の気象データ検索」への直接リンク**になっています。クリックするとその地点を選択済みの状態で開きます（フェーズ12。それまでは日別値ページ`daily_s1.php`へ直リンクしていましたが、アメダス1,230地点ではエラーページになっていたため作り直しました）。
- **一覧に標高・緯度・経度・かなを表示**するようにしました（全1,287地点で欠測なしの、統一して比較できる情報）。CSVにも同じ列と気象庁ページURLを追加しています（フェーズ12）。
- **観測要素を要素ごとに色分け表示**します（気温=赤／降水量=青／積雪=藍／風=緑／湿度=紫／日照時間=橙。観測要素フィルタの色ドットが凡例を兼ねます。フェーズ12）。
- **地点名・かな・都道府県によるキーワード検索**、**一覧のページネーション（50件/ページ）**、**テーブルヘッダーの固定表示**を追加しました。
- **現在の絞り込み結果を地図（Leaflet）上に表示**できます。気象官署／アメダスを色分けし、マーカークリックで地点名・観測要素・気象庁リンクをポップアップ表示します。地域・観測要素・検索の絞り込みを変えると、一覧と同様に地図側も連動して更新されます（全国選択時など件数が多い場合は Leaflet.markercluster でクラスタリング表示）。
- **絞り込み条件をURLクエリに反映**します（地域・観測要素・AND/OR・検索語・ページ番号）。ブラウザのアドレスバーのURLをそのままコピーして開くと、同じ絞り込み条件が復元されます（共同研究者への共有や、条件をブックマークしておく用途を想定）。絞り込みなしの状態はパラメータの付かないシンプルなURLのままです。ステータスバーにあった専用の「この条件のURLをコピー」ボタンは、アドレスバーのURLで代用できるため、フェーズ24で削除しました。
- **観測所の種別（気象官署／アメダス）で絞り込める**ようになりました（独立した絞り込み軸。地域・観測要素とは独立してAND合成される）。

### 実データ整備の方法（フェーズ5）

観測所マスタは気象庁が公式に公開しているアメダス観測所表
（[amedastable.json](https://www.jma.go.jp/bosai/amedas/const/amedastable.json)、全国1,286地点）を元に、以下の手順で再構築しています（`build_stations.py`）。

1. **座標変換**: `[度, 分]` 形式の緯度経度を10進度に変換
2. **都道府県判定**: [dataofjapan/land](https://github.com/dataofjapan/land) の都道府県境界GeoJSON + `shapely` によるpoint-in-polygon判定で、
   緯度経度から都道府県を正確に特定（海岸線付近でポリゴン外に出てしまう17地点のみ、最近傍の都道府県で補完）
3. **地方の割り当て**: 判定した都道府県を、既存の地方マスタ（北海道・東北・関東・中部・近畿・中国・四国・九州・沖縄）で逆引き
4. **観測要素の判定**: `elems`（8桁: 気温・降水量・風向・風速・日照時間・積雪深・湿度・気圧の順）から、
   このアプリが扱う6要素（気温・降水量・風・積雪・湿度・日照時間）を導出（風向・風速はどちらか一方でも観測していれば「風」として扱う。気圧は対象外）
5. **種別の単純化**: `type: "A"`（気象官署）は「気象官署」、それ以外（B〜G、特別地域気象観測所・アメダス等）は「アメダス」として表示
6. **追加観測所のマージ**（フェーズ11）: `amedastable.json` は国内アメダス観測所の表なので南極・昭和基地を含まない。
   `scripts/extra_stations.json` に手書きで定義しておき、再生成のたびに追記する（地方「南極」もこのファイルで定義）。
   昭和基地は気象庁「過去の気象データ検索」(etrn) 上では「昭和（南極）」として `prec_no=99` / `block_no=89532` で公開されており、
   観測要素は気温・風・日照時間・積雪・湿度（降水量は地吹雪のため観測しておらず、etrnの日別値でも常に空欄のため含めていない）

全都道府県で1件以上のデータが確認できています（0件になった都道府県はなし）。処理スクリプト `scripts/build_stations.py` をリポジトリに同梱しているので、気象庁が観測所表を更新した際は同じ手順で再生成できます（`amedastable.json` を再取得し、`python3 scripts/build_stations.py` を実行）。

### まだ対応していない点（既知の制約）

- **precNo/blockNoについて**: 全1,287地点のうち3地点（つくば・奥日光・南大東）は、気象庁側の表記ゆれ（例:「つくば」の正式名称は「つくば（館野）」）が原因で自動マッチに失敗していましたが、`merge_block_numbers.py`に前方一致による救済ロジックを追加し解消しました。残る7地点（弟子屈・新島・神津島・高遠・三倉・宇奈月・有川）は気象庁の地点選択ページ自体に同じ地点名で2つのblock_noが存在していましたが、これは重複ではなく「観測要素の追加（雨量のみ→気温・風などを追加した4要素化）」または「観測所の移転（統計を連続させない番号切り替え）」による新旧番号の切り替えであることを手動確認で特定できました。現在有効な新番号側を`precNo`/`blockNo`として確定し、7地点全てのリンク生成が解消しています（全1,287地点でリンク確定）。`merge_block_numbers.py`は、すでに`precNo`/`blockNo`が確定済みの観測所（`scripts/extra_stations.json`由来、および今回手動確認した7地点）を再照合の対象から除外するため、再実行してもこの結論は保持されます。
- **廃止済み観測所（506地点）は、気象庁の「約200地点」とは数え方が異なります**。obsdlヘルプの「約200地点」はダウンロードツール上で選択できる地点の話で、恐らく実用性を考えて絞り込まれた一覧です。一方こちらは`amdmaster.index`（アメダス地点情報履歴ファイル）の全履歴から「番号の付け替えではなく本当に運用を終えた」地点を機械的に洗い出した数なので、1974年のアメダス発足直後に数年で役目を終えた試験的な地点なども含み、結果的に「約200」より多くなっています。
- **廃止済み観測所506地点のうち、precNo/blockNoが確定しているのは386地点**です（フェーズ19）。機械的な突き合わせ（`block_numbers.json`との一致361地点＋2017年時点の観測地点一覧との突き合わせ6地点）に加え、残り139地点をユーザーが気象庁の地点選択ページで直接目視確認した結果、(b) 同名の観測所が複数の時期にまたがるケース（19地点）は「候補のblockNoで合っている」ことが確認できたため確定しました。(a) 現役観測所と同名のケース（99地点）は地図上に現役の地点しか表示されておらず現役の番号のままで正しいこと、(c) 該当なしのケース（21地点）は地図上に該当する地点自体が存在しないことが確認できたため、この120地点は今後も未確定（気象庁ページへのリンクなし）として扱います。


### 地域選択の動作仕様

- 何も選択しない状態 = 絞り込みなし（全観測所を表示）
- 地方チェックボックスをON/OFF → 配下の全都道府県が連動してON/OFF
- 都道府県を個別にON/OFFすると、所属する地方チェックボックスは自動的に
  「一部選択（indeterminate）」「全選択」「未選択」のいずれかに更新される
- 「すべて一括選択」チェックボックスも同様に、全体の選択状況を反映する
  （北海道の14地域＋他46都道府県＋南極を合わせた61地域が対象。南極には昭和基地1地点が属する。フェーズ23で
  北海道が1都道府県から14地域に分割されたため、対象数は48→61に変わった）
- 「47都道府県一括選択」チェックボックスは、南極を除いた60地域（北海道14地域＋他46都道府県）だけを対象に
  同様の一括選択・indeterminate表示を行う（南極だけを選択している場合はこちらは未選択のまま。フェーズ21・23）
- 「選択をクリア」ボタンで全解除できる
- 各項目の「(件数)」は、観測要素・種別・検索語による絞り込みを反映した件数を表示する（後述「件数表示の連動」）
- **北海道は宗谷・上川・留萌・石狩・空知・後志・オホーツク・胆振・日高・十勝・釧路・根室・渡島・檜山の
  14地域（振興局相当）に分割されている**（フェーズ23）。地域選択パネルには「北海道」1枚のカードとして
  表示され、その中に14地域分のチェックボックスが並ぶ（フェーズ23時点では「北海道（道北・道東）」
  「北海道（道央・道南）」の2枚に分けていたが、フェーズ24で1枚に統合した）。station.prefecture自体は
  「北海道」のままで変わらず（CSV出力・気象庁リンク・キーワード検索・「地方」列の表示に影響しない）、
  地域選択・件数集計・絞り込みだけが`filterEngine.js`の`regionSelectorKey()`を通じて、各観測所のprecNo
  （気象庁の府県予報区番号。11〜24が北海道内の14地域に対応）から地域名に変換した値で行われる。
  precNo未確定の廃止済み観測所（17地点）はどの地域にも分類されないため、地域を絞り込むと選択対象から
  外れる（絞り込みなしの状態では今まで通り表示される）

### 観測要素フィルタの動作仕様

- 何も選択しない状態 = 絞り込みなし（全観測所を表示）
- 観測要素（気温・降水量・積雪・風・湿度・日照時間）を複数チェックすると、
  「すべて含む（AND）」「いずれか含む（OR）」を切り替えて絞り込める
- モードを切り替えても選択中の要素は維持される
- 「選択をクリア」ボタンで要素の選択のみ解除できる（モードは維持）。ボタンはパネルタイトル横の
  「i」アイコンの右隣に配置している（フェーズ23。以前はパネル本文内のAND/OR切り替えの隣にあった）

### 件数表示の連動（フェーズ11）

地域・観測要素・種別の各選択肢に付く「(件数)」は、**その軸自身の選択を無視し、他の軸の条件だけを適用した母集団**の件数を表示する（一般的なファセット検索と同じ数え方。`filterEngine.js` の `buildFacetCounts()`）。

- 地域で北海道だけを選ぶ → 観測要素・種別の件数が北海道内の件数になる
- 観測要素で積雪を選ぶ → 都道府県の件数が積雪観測地点だけの件数になる（例: 沖縄県は0件表示になり、薄いグレーで表示される）
- すでに選んでいる項目の件数が、自分自身の選択のせいで減って見えることはない
- キーワード検索は「軸」ではなく常時適用の条件として扱うため、すべての件数に効く
- 件数の更新はDOMのラベル文字列だけを差し替える方式で、チェック状態や地方グループの開閉状態は保持される

### 観測所一覧の動作仕様（フェーズ12で刷新）

- 列構成: 地点コード／地点名／かな／都道府県／地方／標高(m)／緯度／経度／観測要素／種別。
  標高・緯度・経度は全1,287地点で欠測が無く、地点同士を統一した基準で比較できる情報として表示している
  （緯度経度は小数第4位＝約10m精度まで表示）
- **地点名が気象庁「過去の気象データ検索」(etrn) へのリンク**になっており、クリックするとその地点を
  選択済みの状態でページが開く。年月とデータ種別（日別値・月別値・平年値など）はそのページで選ぶ
- 地点番号（`precNo`/`blockNo`）は全1,287地点で確定済みのため、全地点がこの地点別リンクになる
  （かつて7地点は新旧番号の候補が特定できず都道府県ページへの案内に留めていたが、フェーズ14で解消した）
- 観測要素は要素ごとに色分けしたタグで表示する（気温=赤／降水量=青／積雪=藍／風=緑／湿度=紫／日照時間=橙）。
  「観測要素で絞り込む」パネルの各項目に同じ色のドットを置いてあり、これが凡例を兼ねる

#### リンク先を etrn のトップ(index.php)にしている理由

日別値ページへの直リンク（`view/daily_s1.php`）は次の2点で全観測所共通の形にできず、実際にアメダス1,230地点で
「ページを表示することが出来ませんでした」になっていた（フェーズ12で判明・修正）。

- 気象官署は `daily_s1.php`、アメダスは `daily_a1.php` と種別ごとにファイルが異なる
- `year` / `month` を空にするとどちらの形式でもエラーページになる

`stats/etrn/index.php?prec_no=..&block_no=..` は種別に関係なく地点が選択された状態で開き、年月・データ種別を
そこから選べるため、こちらを採用している。

### CSVエクスポート・気象庁公式ページの動作仕様

- 「選択結果をCSVでダウンロード」ボタンで、**現在の絞り込み条件で表示中の観測所一覧**をCSVとして保存できる
  （列: 観測所ID・地点名・かな・都道府県・地方・緯度・経度・標高(m)・種別・観測要素・気象庁ページURL）
- 「気象庁の公式ページを開く」ボタンは、気象庁「過去の気象データ・ダウンロード」のトップページを新しいタブで開く。
  同ツールはJS制御のフォームで観測所・項目・期間を選ぶ方式のため、URLでの観測所事前選択（ディープリンク）には対応していない
  （調査結果。詳細は `js/modules/exporter.js` のコメント参照）

### 地図表示の動作仕様

- 地域・観測要素・検索の絞り込み結果（一覧テーブルと同じ`visibleStations`）を地図上にマーカー表示する
- マーカーの色は種別で区別: 気象官署=青緑、アメダス=橙（凡例をパネル下部に表示）
- マーカーをクリックすると、地点名・かな・都道府県・種別・標高・緯度経度・地点コード・観測要素・気象庁リンクをポップアップ表示する
  （気象庁リンクは一覧と同じ扱い。地点番号は全地点で確定済みのため、常に地点別リンクを表示する）
- **地図の拡大縮小・移動には Ctrl（macOSは⌘）キーが必要**（フェーズ11）
  - Ctrl＋ホイールで拡大縮小、Ctrl＋ドラッグで移動。Ctrlを押していないときはページが通常どおりスクロールし、地図の上に操作ヒントを一時表示する
  - タッチ端末では1本指の操作はページのスクロール、2本指で地図の拡大縮小・移動になる
  - Ctrl無しで使える操作: 左上の ＋／− ボタン、ダブルクリックでの拡大、右上の「検索結果に合わせる」ボタン、地図フォーカス中の矢印キー
  - 実装上は Leaflet の `scrollWheelZoom` / `dragging` を無効にしたうえで、修飾キーの押下状態に応じて自前でズーム（`setZoomAround`）とドラッグ有効化を行っている（「Ctrl検知後にLeafletのハンドラを有効化」ではその回のイベントがハンドラに届かず、1回目だけブラウザのページ拡大が起きてしまうため）
- 絞り込み条件を変えると、表示範囲（ズーム・中心）は対象観測所が収まるよう自動調整される。
  ただし**絞り込みが無い（全観測所表示）ときは表示範囲を動かさない**（南極まで含めて範囲を合わせると地図が世界全体まで引いてしまうため。日本全体が収まる初期表示のままにする）
- 右上の「検索結果に合わせる」ボタンで、いつでも現在の絞り込み結果全体が収まる位置に戻せる
- 全国選択時など表示件数が多い場合は、近接するマーカーをクラスタ化して表示する（Leaflet.markercluster使用。ズームすると自動的に個別マーカーに分解される）
- ページネーションでページを送っても地図は再描画されない（地図は「絞り込み結果全体」を表示する仕様のため、テーブルのページングとは独立している）
- Leaflet / Leaflet.markercluster は`vendor/leaflet/`にリポジトリ同梱しており、外部CDNには依存しない（フェーズ18）。`vendor/leaflet/`が欠けている・壊れている場合のみ地図パネルにエラーメッセージを表示する（他機能には影響しない）
- **マーカー⇔一覧行の相互連携**（フェーズ15）: 地図のマーカーをクリックすると、対応する一覧の行がある位置までページが自動的に切り替わり、その行がハイライト＆スクロール表示される。逆に一覧の行をクリックすると、地図がその地点へパンしてポップアップが開く（クラスタ化されている場合はクラスタを解いてから開く）。マーカーと行はどちらも`store.selectedStationId`という共通の状態を介してやり取りしており、絞り込み条件を変えたり手動でページを送ったりすると選択状態は解除される

### URLクエリ化の動作仕様

- 地域（`pref`）・観測要素（`elem`）・AND/OR（`mode`）・検索語（`q`）・ページ番号（`page`）を、絞り込みのたびに`history.replaceState`でURLへ反映する（`pushState`は使わないため、フィルタ操作のたびに「戻る」履歴が積み上がることはない）
- 既定値のパラメータ（絞り込みなし・AND・1ページ目）は省略され、URLはできるだけ短く保たれる。何も絞り込んでいない状態はパラメータの付かないトップページURLと同じになる
- 上記URLで直接アクセスすると、ページ読み込み時にクエリを解析し、地域選択・観測要素選択・AND/OR・検索ボックス・ページ番号・一覧・地図のすべてに同じ条件を復元する
- 都道府県名や検索語に含まれる日本語は`URLSearchParams`が自動的にパーセントエンコードする（ブラウザのアドレスバー上では大抵の場合デコードされた日本語表示になる）

### 種別フィルタの動作仕様

- 「種別で絞り込む」パネルで気象官署／アメダスを選択できる。地域・観測要素とは独立した絞り込み軸で、他の条件とはAND合成される（何も選択しない場合は絞り込みなし＝両方表示、他の絞り込みUIと同じ方針）
- URLクエリでは`type=kansho,amedas`のように短いコードで表現される
- 「選択をクリア」ボタンはパネルタイトル横の「i」アイコンの右隣に配置している（フェーズ23）

> フェーズ9で追加した「プリセットで絞り込む」パネル（気象官署のみ／積雪観測地点のみ 等）は、
> 個別の絞り込みパネルで同じことができ画面を占有するだけだったため、フェーズ12で削除した
> （`js/modules/presets.js` / `presetPanel.js` / `test-presets.mjs` も併せて削除）。

### 廃止済み観測所の動作仕様（フェーズ16・17・19・20・21）

- 現在は運用されていない過去の観測所506地点は、**既定で**一覧・地図・件数集計に含まれる（フェーズ21で既定を
  反転。従来はオプトインで既定オフだった）。「廃止済み観測所」パネルのチェックボックスは「廃止済みの観測
  地点を含めない」というオプトアウトの向きになっており、オンにするとこの506地点が母集団から除外される
- チェックボックスの（　）内の件数は固定値ではなく、他の絞り込み条件（地域・観測要素・種別・検索語）に
  連動して変化する（例: 北海道だけを選ぶと北海道内の廃止済み観測所の件数になる。フェーズ21）
- 廃止済み観測所は`data/stations.json`の`discontinuedStations`に、現役の`stations`とは別の配列として収録している。
  地域・観測要素・種別・検索のいずれの絞り込みも、「含めない」がオフ（既定）のときはこの506地点を母集団に
  含める（オンのときは他の絞り込みUIの件数・一覧・地図から完全に除外される）
- 一覧では地点名の隣に「廃止」タグを表示し、その下に観測期間（例:「観測期間: 1961年1月〜2002年2月」）を表示する。
  precNo/blockNoが確定している361地点は地点名が現役観測所と同じように気象庁ページへのリンクになる
  （フェーズ19・20）。残る120地点は地点番号が未確定のためリンクにならない
- 地図ではマーカーの色を種別に関わらずグレーで統一表示する（凡例に「廃止済み」を追加）。ポップアップには
  常に観測期間を表示し、precNo/blockNoが確定していれば気象庁リンクも併記する。未確定の場合はリンクの
  代わりに「地点番号未確定のためリンクはありません」と案内する
- CSVエクスポートには「状態」（現役／廃止済み）と「観測期間」の列を追加している
- チェック状態はURLクエリ`discontinued=0`（含めない）に反映され、共有リンクからも復元できる。既定（含める）
  のときはパラメータを省略しURLを短く保つ（フェーズ21でパラメータの意味を反転）
- データの出どころ: 気象官署・測候所12地点は気象庁の観測所履歴ファイル`smaster.index`（気象庁側に同名で
  複数の地点番号が存在するのと同様、アメダス網の外にある独立した観測所のため後述のアメダス履歴には出てこない）。
  アメダス494地点は気象庁純正の**アメダス地点情報履歴ファイル`amdmaster.index`**（全履歴7,935レコード）を
  解析し、番号の付け替え（同一地点が新しい番号で運用を続けているケース）を`Old Station Number`列で
  追跡したうえで、本当に運用を終えた地点だけを抽出している。緯度経度・標高・観測要素（気温/降水量/風/
  日照時間/積雪/湿度）は同ファイルの記載値をそのまま使用しており、都道府県の判定は`scripts/build_stations.py`
  と同じ方式（境界GeoJSON + point-in-polygon）で行っている

### 検索・ページネーションの動作仕様

- 検索ボックスに入力すると、地点名・かな・英語名・都道府県名のいずれかに部分一致する観測所だけに絞り込まれる
  （大文字小文字は区別しない。入力が止まってから200ms後に反映されるデバウンス処理付き）
- 検索・地域選択・観測要素フィルタはすべて組み合わせて使える（AND条件で合成）
- 一覧は50件ずつページ分割して表示する。地域・観測要素・検索条件を変更すると、自動的に1ページ目に戻る
- テーブルヘッダーは縦スクロールしても常に見える位置に固定表示される

## ローカルでの動作確認

`index.html` を直接ダブルクリックで開くと、ブラウザのセキュリティ制限（CORS）により
`fetch("data/stations.json")` が失敗します。必ず簡易サーバー経由で開いてください。

```bash
cd weather-station-finder
python3 -m http.server 8000
# その後ブラウザで http://localhost:8000 を開く
```

Node.js がある場合は `npx serve` でも構いません。

## 動作テスト

地域選択・観測要素フィルタ・件数の連動集計・CSVエクスポート・気象庁リンク生成・検索/ページネーション・
マーカー⇔一覧行の相互連携・廃止済み観測所の表示、および実際にHTML+JSを動かす統合スモークテストを、jsdom を使った自動テストで検証しています
（`test-region-selector.mjs` / `test-element-filter.mjs` / `test-facet-counts.mjs` /
`test-exporter.mjs` / `test-search-pagination.mjs` / `test-map-selection.mjs` / `test-discontinued.mjs` /
`test-integration-smoke.mjs`）。

```bash
cd weather-station-finder
npm install
npm test
```

## ファイル構成

```
weather-station-finder/
├── index.html              # ページ本体
├── vendor/
│   └── leaflet/            # Leaflet / Leaflet.markercluster の同梱ファイル（npm配布物。フェーズ18でCDN依存を解消）
├── css/
│   ├── base.css            # デザイントークン・リセット
│   ├── layout.css          # ヘッダー・グリッド
│   ├── components.css      # テーブル・検索・ページネーション等の部品
│   └── map.css             # 地図パネル・Leafletポップアップ（フェーズ6・実装済み）
├── js/
│   ├── main.js              # エントリーポイント
│   ├── data/                # （今後）データ関連の補助モジュール置き場
│   ├── modules/
│   │   ├── stateManager.js    # 状態管理（Pub/Sub）
│   │   ├── stationList.js     # 一覧テーブル描画
│   │   ├── regionSelector.js  # 地域一括選択（フェーズ2・実装済み）
│   │   ├── elementFilter.js   # 観測要素絞り込み・AND/OR切り替え（フェーズ3・実装済み）
│   │   ├── keywordSearch.js   # 検索ボックス（フェーズ5・実装済み）
│   │   ├── pagination.js      # ページネーション（フェーズ5・実装済み）
│   │   ├── filterEngine.js    # 絞り込み条件の合成ロジック（地域/要素/検索）
│   │   ├── exporter.js        # CSV出力・気象庁公式ページへのリンク生成（フェーズ4・実装済み）
│   │   ├── mapView.js         # 地図表示・Leaflet連携。一覧行との相互連携（フェーズ6・フェーズ15）
│   │   ├── urlState.js        # 絞り込み状態⇔URLクエリの相互変換（フェーズ7・実装済み）
│   │   ├── typeFilter.js      # 種別（気象官署／アメダス）絞り込みUI（実装済み）
│   │   └── discontinuedFilter.js # 「廃止済み観測所を含める」チェックボックスUI（フェーズ16）
│   └── utils/
│       └── helpers.js
├── data/
│   └── stations.json        # 観測所マスタデータ（実データ、全国1,286地点＋南極1地点。廃止済み観測所506地点は discontinuedStations に別枠で収録。フェーズ16・17）
├── scripts/
│   ├── build_stations.py    # amedastable.json から stations.json を生成するスクリプト
│   └── extra_stations.json  # amedastable.json に載らない観測所の定義（南極・昭和基地。フェーズ11）
├── fetch_block_numbers.py   # etrn から precNo/blockNo を収集するスクリプト
├── merge_block_numbers.py   # precNo/blockNo を stations.json にマージするスクリプト
├── test-region-selector.mjs   # 地域選択ロジックの自動テスト（jsdom使用）
├── test-element-filter.mjs    # 観測要素フィルタの自動テスト（jsdom使用）
├── test-exporter.mjs          # CSV/リンク生成ロジックの自動テスト
├── test-search-pagination.mjs # 検索・ページネーションの自動テスト
├── test-map-view.mjs          # 地図マーカー色・ポップアップHTML生成の自動テスト（フェーズ6）
├── test-url-state.mjs         # URLクエリの読み書きロジックの自動テスト（フェーズ7）
├── test-type-filter.mjs       # 種別フィルタUIの自動テスト（フェーズ9）
├── test-facet-counts.mjs      # 件数の連動集計・南極データ収録の自動テスト（フェーズ11）
├── test-map-selection.mjs     # マーカー⇔一覧行の相互連携の自動テスト（フェイクLeaflet使用。フェーズ15）
├── test-discontinued.mjs      # 廃止済み観測所のデータ整合性・UI・地図/一覧表示の自動テスト（フェーズ16）
├── test-integration-smoke.mjs # index.html + main.js を実際に動かす統合スモークテスト（jsdom使用）
└── package.json
```

## データについて（重要）

`data/stations.json` は **プロトタイプ用のサンプルデータ** です。
各都道府県の代表的な観測地点を1件ずつ、大まかな座標・観測要素で収録していますが、
実運用前に気象庁の最新の観測所一覧と照合し、正確なデータに差し替える必要があります。

このツールは気象庁の生観測データを再配布するものではなく、観測所を探すための
メタデータ（地点名・地方・都道府県・観測要素など）のみを扱う設計です。
実際の気象データ取得は、最終的に気象庁の該当ページへ案内する形を想定しています。

## 今後の開発フェーズ

- ~~**フェーズ2**: 地方・都道府県単位の一括選択（階層チェックボックス）~~ ✅ 完了
- ~~**フェーズ3**: 観測要素による絞り込み（AND/OR切り替え対応）~~ ✅ 完了
- ~~**フェーズ4**: 選択結果のCSVエクスポート、気象庁ページへのリンク生成~~ ✅ 完了
- ~~**フェーズ5**: 全観測所データの整備（`precNo`/`blockNo`含む実データ化）、UI/UXブラッシュアップ（検索・ページネーション・ヘッダー固定）~~ ✅ 完了
- ~~**フェーズ6**: 地図表示（Leaflet、気象官署/アメダス色分け、マーカークラスタリング、絞り込み結果との連動）~~ ✅ 完了
- ~~**フェーズ7**: 絞り込み状態のURLクエリ化（地域・観測要素・AND/OR・検索語・ページ番号を復元可能に、「URLをコピー」ボタン）~~ ✅ 完了
- ~~**フェーズ8**: precNo/blockNo未収録10地点の手動補完（表記ゆれ3件は自動解消、重複block_no 7件は候補を記録し保留）~~ ✅ 完了
- ~~**フェーズ9**: 絞り込み条件のプリセット機能（気象官署のみ／アメダスのみ／気温・降水量ともに観測 等をワンクリック適用、種別フィルタ軸を新設）~~ ✅ 完了
- ~~**フェーズ10**: 本番品質への仕上げ~~ ✅ 完了
  - ユーザー向け表示から内部的な「フェーズN」表記を除去（開発時の進捗管理用の呼び方であり、利用者には無関係なため）
  - ステータスバー・観測所一覧の説明文を実際のデータ状態（1,279/1,286地点でリンク確定、残り7件は候補あり表示）に合わせて修正
  - ステータス件数表示に`aria-live="polite"`を付与し、絞り込み結果の変化をスクリーンリーダーでも把握できるように
  - Leaflet / Leaflet.markercluster のCDN読み込みに、npm配布物から算出した実際のSRI（sha384）ハッシュを付与し、CDN側の内容改ざんに対する保護を追加
  - `.gitignore`を追加（`node_modules/`等が誤ってコミットされることを防止）
- ~~**フェーズ11**: 操作性の改善・南極追加~~ ✅ 完了
  - 地図の操作にCtrl（macOSは⌘）を要求するジェスチャ制御を追加（ページスクロール中に地図が勝手に動く問題の解消）。併せて「検索結果に合わせる」ボタンを追加し、絞り込みが無いときは表示範囲を自動調整しないようにした
  - 南極・昭和基地（prec_no=99 / block_no=89532）を観測所マスタに追加し、地方「南極」を新設。再生成時にも失われないよう `scripts/extra_stations.json` に定義を置き、`build_stations.py` / `merge_block_numbers.py` を対応させた
  - 地域・観測要素・種別の「(件数)」を、他の絞り込み条件に連動して更新するように変更（`buildFacetCounts()`）。0件になった選択肢は薄いグレーで表示する
- ~~**フェーズ12**: 一覧の情報量改善~~ ✅ 完了
  - 「プリセットで絞り込む」パネルを削除（個別パネルで同じ絞り込みができるため）
  - 一覧の「気象庁」列を廃止し、地点名自体をリンク化。リンク先を`view/daily_s1.php`から`stats/etrn/index.php`（地点選択済みのトップ）へ変更し、アメダス1,230地点でエラーページになっていた問題を解消。地点番号未確定の7地点も都道府県ページへ案内し、全地点が有効なリンクになった
  - 一覧に かな・標高(m)・緯度・経度 を追加（全地点で欠測なし）。CSVにも同じ列＋気象庁ページURLを追加
  - 観測要素を6要素それぞれ色分けし、観測要素フィルタの色ドットを凡例とした
- ~~**フェーズ14**: 残り7地点のblock_no手動確定~~ ✅ 完了
  - フェーズ8で候補記録のまま保留していた7地点（弟子屈・新島・神津島・高遠・三倉・宇奈月・有川）について、気象庁の地点選択ページで手動確認を実施
  - 2つのblock_no候補は重複ではなく、観測要素の追加（雨量のみ→4要素化）または観測所の移転（統計切断）による新旧番号の切り替えと判明。現在有効な新番号側を`precNo`/`blockNo`として確定した
  - `merge_block_numbers.py`は、すでに`precNo`/`blockNo`が確定済みの観測所（`scripts/extra_stations.json`由来、および今回手動確認した7地点）を再照合の対象から除外するように変更し、再実行時に手動確認の結論が上書きされないようにした
  - 結果、全1,287地点で`precNo`/`blockNo`が確定し、一覧・地図とも例外なく地点別リンクを表示するようになった
- ~~**フェーズ15**: マーカー⇔一覧行の相互連携~~ ✅ 完了
  - 地図のマーカーをクリックすると、対応する一覧の行が表示されるページへ自動的に切り替わり、ハイライト＆スクロール表示される
  - 一覧の行をクリックすると、地図がその地点へパンしてポップアップを開く（クラスタ化中はクラスタを解いてから開く）
  - 両者は`store.selectedStationId`という共通の状態でつながっており、絞り込み変更や手動でのページ送りでは選択を解除する
- ~~**フェーズ16**: 廃止済み観測所の追加（第一弾）~~ ✅ 完了 → フェーズ17でデータを全面的に作り直し
  - 気象庁「過去の気象データ・ダウンロード」ヘルプ（`/risk/obsdl/top/help1`）に「約1,500地点（現在観測を行っていない約200地点を含む）」という記述があるが、`data/stations.json`の元データ（アメダス観測所表`amedastable.json`）は現役ネットワークのみを収録しており、この約200地点は一件も含まれていない
  - ユーザーから提供いただいた資料（気象官署・測候所の履歴ファイル`smaster.index`、アメダス観測所履歴を要約したPDF）から、まず21地点（気象官署・測候所12地点＋アメダス9地点）を`discontinuedStations`として追加した
  - 一覧の「廃止」タグ・観測期間表示、地図のグレーマーカー、CSVの「状態」「観測期間」列、URLクエリ`discontinued=1`など、UI・データ構造の土台をここで作った
- ~~**フェーズ17**: 廃止済み観測所データの正確化・大幅拡充~~ ✅ 完了
  - ユーザーから気象庁純正の**アメダス地点情報履歴ファイル（`amdmaster.index`の完全版CSV、7,935レコード）**が提供され、これを解析した
  - 地点番号ごとに観測開始日・終了日・観測要素（気温/降水量/風/日照時間/積雪/湿度をフラグで明示）の全履歴が入っており、フェーズ16で推測に頼っていた部分（観測要素の構成、筑波山・白浜・金武が実際には期間の異なる2レコードに分かれていたこと等）を正確な値に置き換えられた
  - 地点番号は全1,868件（現役1,286件＋廃止1,868-1,286=582件）。ただし582件のうち88件は「番号が変わっただけで観測は継続」（例: 札幌14161→14162→14163、旭川12441→12442）と判明したため、`Old Station Number`列を使って後継地点への付け替えを追跡し、本当に廃止された**494件**を特定した
  - 都道府県の判定は`scripts/build_stations.py`と同じ方式（[dataofjapan/land](https://github.com/dataofjapan/land)の境界GeoJSON + point-in-polygon、海岸部5件は最近傍で補完）で行った
  - フェーズ16の気象官署・測候所12地点（`smaster.index`由来。アメダス網の外にある独立した観測所のため今回のアメダス履歴には含まれない）はそのまま残し、494件と合わせて**廃止済み観測所は合計506地点**になった
  - 山頂の測候所（筑波山・伊吹山・剣山・阿蘇山）、1902年の大噴火で職員全員が犠牲になった鳥島に加え、1974年のアメダス発足直後に試験的に置かれ数年で役目を終えた地点なども含む
  - 地点番号（precNo/blockNo）は今回もetrn側の対応が未確認のため引き続き未設定。標高は`amdmaster.index`の値をそのまま収録した
- ~~**フェーズ18**: 地図のCDN依存を解消~~ ✅ 完了
  - Leaflet / Leaflet.markercluster を`npm install`で取得し、`dist`配下の必要なファイル（本体JS/CSS・マーカー画像・クラスタリングのCSS/JS）を`vendor/leaflet/`としてリポジトリに同梱した
  - `index.html`の参照先を`https://unpkg.com/...`から`vendor/leaflet/...`に変更し、SRI（integrity/crossorigin属性）は同一オリジンの自前ファイルのため撤去した
  - これにより、外部ネットワークが遮断された環境（社内LAN・CDNブロックなど）でも地図が表示されるようになった。バージョンを上げる場合は`npm install leaflet@<version> leaflet.markercluster@<version>`のうえ`vendor/leaflet/`を再コピーする
- ~~**フェーズ19**: 廃止済み観測所のprecNo/blockNo確認~~ ✅ 完了
  - ユーザーが気象庁の地点選択ページ（`prefecture.php`の地図リンク）から`document.querySelectorAll('map area')`でprec_no/block_noを収集する方法を教えてくれた。これは`fetch_block_numbers.py`が使っているのと同じページで、実は既存の`data/block_numbers.json`に廃止済み観測所の情報も紛れ込んでいることが分かった（1,678件収録されており、現役1,287地点より391件多い）
  - 廃止済み506地点を地点名・都道府県で`block_numbers.json`と突き合わせたところ485件が一致した。ただしそのうち105件は**現役観測所と同名**で、一致したblock_noが現役側に既に設定済みの値と完全一致することを確認（＝廃止済み地点自身の番号ではなく現役側を指している）。さらに19件は同名の観測所が複数の時期（世代）にまたがっており、1つのblock_noがどの世代を指すか特定できなかった
  - これらの紛らわしいケースを除いた361地点でprecNo/blockNoを確定した
  - `mapView.js`のポップアップ表示を修正し、廃止済み観測所でもprecNo/blockNoが確定していれば通常の観測所と同じ気象庁リンクを表示しつつ、「廃止済み観測所（観測期間: ...）」の注記も併記するようにした（`stationList.js`側は元々precNo/blockNoの有無だけでリンク要否を判定していたため、追加の変更なしで正しく動作した）
  - さらにユーザーから2017年時点の観測地点一覧（個人ブログ公開のcsvをtxt化したもの、1,664地点）が提供された。これを未確定の地点と突き合わせたところ、フェーズ8/14で新旧番号の切り替えと判明していた7地点のうち6地点（弟子屈・神津島・高遠・三倉・宇奈月・新島のアメダス期）について、「2候補のうち現役側が使っていない方」を消去法で確定できた（残る1件・新島の測候所期（1961〜1966年）はアメダス制度開始(1974年)より前に廃止されているため対象外とした）
  - この時点で367地点でprecNo/blockNoを確定。残り139地点（現役同名99・複数世代19・該当なし21）はCSV（`discontinued_stations_to_fill.csv`）に書き出し、ユーザーに手動確認を依頼した
- ~~**フェーズ20**: 残り139地点の手動確認結果を反映~~ ✅ 完了
  - ユーザーが気象庁の地点選択ページで139地点全てを目視確認した結果: (b) 複数世代の19地点は「候補のblockNoで合っている」ことを確認（同じ物理的な場所を指すため、地図上のマーカーは1つしかなく、どの世代を指すか気にせずそのマーカーの番号でよいと判断）。(a) 現役同名の99地点は「地図上に現役の地点しか無い」ことを確認（＝廃止済み地点自身の番号は既に残っていない）。(c) 該当なしの21地点は「地図上に該当する地点自体が無い」ことを確認
  - (b)の19地点にblockNoを確定し、廃止済み観測所のprecNo/blockNo確定数は386地点になった。(a)(c)の120地点は気象庁の地点選択ページ自体に情報が残っていないため、引き続き未確定（気象庁ページへのリンクなし）とする
  - 目視確認の記録として使った`discontinued_stations_to_fill.csv`はリポジトリに残している
- ~~**フェーズ21**: UI/UX改善（一括選択・パネル配置・廃止済み観測所の既定表示化 等）~~ ✅ 完了
  - 地域選択に「47都道府県一括選択」を追加。従来の「すべて一括選択」（南極含む48地域）とは別に、日本国内の47都道府県だけを一括選択できるようにした（`regionSelector.js`）
  - 「観測要素で絞り込む」パネル下の余白を解消するため、「種別で絞り込む」「廃止済み観測所」パネルを右側の縦積みレイアウトに移動した（`index.html` / `css/layout.css`の`.filter-panels__right`）
  - 観測所一覧の観測要素タグを、要素の有無に関わらず6要素分の固定位置に表示するよう変更（CSS Gridと非表示プレースホルダーを使用）。従来は観測要素が少ない地点でタグが左詰めになり列がずれて見えていた（`stationList.js`の`renderElementTags`、`css/components.css`の`.element-tags`）
  - 廃止済み観測所の既定を「含めない」から**「含める」に反転**した。チェックボックスは「廃止済みの観測地点を含めない」というオプトアウトの向きに変わり、（　）内の件数も他の絞り込み条件（地域・観測要素・種別・検索語）に連動して変化するようにした（`discontinuedFilter.js`・`stateManager.js`・`main.js`）。URLクエリ`discontinued`パラメータの意味も反転し、既定（含める）のときは省略、明示的に除外するときだけ`discontinued=0`を付与するようにした（`urlState.js`）
  - 湿度・日照時間の観測地点数が現役観測所のみでは844件ずつで完全に一致していたため列の統合を検討したが、廃止済み観測所を含めた既定状態では日照時間942件・湿度857件とズレて観測地点の集合も一致しなくなることが判明したため、統合は見送り現状の個別表示を維持することにした
- ~~**フェーズ22**: レイアウト改善（地域パネルの5×2化・パネル配置・説明文のホバー表示・地図既定表示の修正）~~ ✅ 完了
  - 「地域で絞り込む」パネルを横3×縦4（`.region-list`のgrid-template-columns: repeat(3, 1fr)）から横5×縦2（repeat(5, 1fr)）に変更した（`css/components.css`）
  - パネル配置を再構成: 地域パネルを全幅の1段目に、観測要素・種別・廃止済み観測所の3パネルをその下に横並びの1段（`.filter-panels__row`）で配置するようにした（フェーズ21の右側縦積みレイアウトから変更。`index.html` / `css/layout.css`）
  - 各パネルの説明文（`.panel__note`）を既定で非表示にし、タイトル横に追加した「i」アイコン（`.panel__info-icon`）にカーソルを合わせる／フォーカスすると吹き出しとして表示するようにした。CSS のみで実装（`.panel__info:hover .panel__note` 等）し、キーボード操作でも`tabindex="0"`＋`:focus-within`で表示できる（`css/layout.css`）
  - 地図の既定表示（絞り込みなしの初期状態）が南極・昭和基地まで含めて世界地図規模にfitBoundsされてしまう不具合を修正。原因は、フェーズ21で廃止済み観測所が既定で母集団に含まれるようになったことで、`mapView.js`内の「絞り込みが効いているか」の判定（`visibleStations.length !== allStations.length`）が、実際には何も絞り込んでいなくても真になってしまっていたため。判定を地域・観測要素・種別・検索語の実際の選択状態だけで行う`hasActiveFilters()`に置き換え、既定表示では表示範囲を動かさない（日本周辺の初期ビューのまま）ようにした（`mapView.js`）
- ~~**フェーズ23**: 地域選択の改善（北海道14地域分割・選択クリアボタンの配置）~~ ✅ 完了
  - 北海道は面積が広く1都道府県のままでは絞り込みの単位として粗すぎたため、気象官署のprecNo（府県予報区番号。11〜24が北海道内に対応）を使って宗谷・上川・留萌・石狩・空知・後志・オホーツク・胆振・日高・十勝・釧路・根室・渡島・檜山の14地域に分割した。`data/stations.json`に新しい`hokkaidoSubAreas`（2グループ×7地域）を追加し、`scripts/build_stations.py`が再生成時も引き継ぐようにした。`station.prefecture`自体は「北海道」のまま変更していないため、CSV出力・気象庁リンク・キーワード検索・「地方」列表示への影響はない
  - `filterEngine.js`に`getHokkaidoArea()`（precNoから地域名を求める）と`regionSelectorKey()`（地域選択の絞り込み・集計に使うキー。北海道は地域名、それ以外は都道府県名）を追加し、`computeVisibleStations()`・`buildPrefectureCounts()`をこのキーで判定するように変更した
  - `regionSelector.js`に`hokkaidoSubAreas`パラメータを追加。渡されると北海道の単一カードの代わりに「北海道（道北・道東）」「北海道（道央・道南）」の2枚（各7地域）を描画する。5×2グリッドではこの1枚増加分だけ近畿が2段目に押し出されるため、選択肢が1件ずつしかない沖縄・南極を`.region-group-stack`で同じ列に半分の高さで重ねて表示し、グリッド全体を10枚に保つようにした
  - 「47都道府県一括選択」「すべて一括選択」の対象件数が、北海道の分割に伴い47/48から60/61に変わった（北海道が1都道府県から14地域になったため）
  - 「観測要素で絞り込む」「種別で絞り込む」パネルの「選択をクリア」ボタンを、パネル本文内からタイトル横の「i」アイコンの右隣に移動した。`elementFilter.js`/`typeFilter.js`に`clearButtonSlot`パラメータを追加し、渡されるとそちらにボタンを描画するようにした（省略時は従来通りパネル本文内に描画され後方互換）
- ~~**フェーズ24**: レイアウト微調整（北海道カードの統合・観測要素/種別/廃止済みパネルの配置・URLコピー機能の削除）~~ ✅ 完了
  - ステータスバーの「この条件のURLをコピー」ボタンを削除した（ブラウザのアドレスバーのURLで代用できるため）。`index.html`から`#copy-link-btn`とその親の`.status-bar__right`を、`main.js`から`copyLinkBtn`の参照とクリックハンドラを、`css/layout.css`から未使用になった`.status-bar__right`のスタイルを削除した
  - 北海道の地域カードを、フェーズ23の「北海道（道北・道東）」「北海道（道央・道南）」2枚組から、宗谷・上川など14地域をまとめて持つ1枚に統合した。`regionSelector.js`の`buildDisplayRegions()`を変更し、`hokkaidoSubAreas`の2グループを`flatMap`で1つの地域リストにまとめてから単一の region オブジェクトとして扱うようにした（`filterEngine.js`側のprecNoベースの絞り込みロジックは変更なし）。5×2グリッドではカードが1枚減った分、近畿が1段目に戻っている
  - 絞り込みパネルの配置を「観測要素で絞り込む」を左列、「種別で絞り込む」「廃止済み観測所」を右列に縦積みにする形に変更した（フェーズ22で導入した3パネル横並びから変更）。`index.html`に`.filter-panels__row-right`のラッパーを追加し、`css/layout.css`の`.filter-panels__row`を`grid-template-columns: repeat(3, 1fr)`から`1fr 1fr`の2カラムに変更した
- ~~**フェーズ25**: レイアウト微調整（沖縄・南極の通常サイズ化・観測要素パネルの高さ揃え）~~ ✅ 完了
  - フェーズ23で北海道が2枚のカードになったことに伴い、5×2グリッドの帳尻合わせとして沖縄・南極を`.region-group-stack`で半分の高さに重ねて表示していたが、フェーズ24で北海道が1枚に統合されグリッドが自然に収まるようになったため不要になった。`regionSelector.js`の`buildDisplayRegions()`から沖縄・南極を特別扱いする`stack`ロジックを削除し、他の地方と同じ通常サイズのカードとして描画するようにした。`css/components.css`の`.region-group-stack`も削除した
  - 「観測要素で絞り込む」パネルの高さを、「種別で絞り込む」＋「廃止済み観測所」を縦積みにした右列の高さに揃えた。`css/layout.css`の`.filter-panels__row`から`align-items: start`を削除し、CSS Gridの既定値`align-items: stretch`に戻すことで、同じグリッド行内の各アイテムが自動的に最も高いアイテム（右列）に高さを合わせるようにした
- ~~**フェーズ26**: ヘッダーを夜空デザインに変更（星の瞬き・流れ星）~~ ✅ 完了
  - `.site-header`の背景を、従来の単色＋等圧線風の装飾ラインから、濃紺〜黒の放射状グラデーションに変更した
  - `.site-header::before`に複数の`radial-gradient`を重ねて小さな光点（星）を散りばめ、`background-size`でタイル状に繰り返し、`twinkle`アニメーション（不透明度を往復）で瞬かせている
  - 流れ星は`.shooting-star`要素3本（`index.html`にaria-hidden付きで配置）。それぞれ角度・長さ・アニメーション周期（9s/14s/17s）と開始位置をずらし、`animation-delay`に負の値を与えて非同期に見えるようにした。周期の90%以上は不透明度0（静止）で、終盤の数%だけ`translate`で斜めに移動しながら現れて消える＝「たまに流れる」演出。`transform: rotate()`（静的な角度）と`translate`（動的な移動量）を別プロパティとして扱うことで、両方を同時に効かせている
  - `prefers-reduced-motion: reduce`環境では瞬き・流れ星のアニメーションを止め、静止した星空にする
